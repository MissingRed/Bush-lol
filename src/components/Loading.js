import React from 'react'

const Loading = () => {
	return (
		<div className="container-error">
			<img className="error" src="/img/loading.gif" alt="error" />
			<span>Cargando...</span>
		</div>
	)
}

export default Loading
