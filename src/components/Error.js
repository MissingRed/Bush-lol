import React from 'react'

const Error = () => {
	return (
		<div className="container-error">
			<img className="error" src="/img/error.gif" alt="error" />
			<span>Ocurrio un error al conectarse con el servidor</span>
		</div>
	)
}

export default Error
